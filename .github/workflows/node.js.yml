# This workflow will do a clean installation of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-nodejs

name: Node.js CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
   - cron: "0 2 * * 1-5"
jobs:
  build:

    runs-on: ubuntu-latest
   
    strategy:
      matrix:
        node-version: [14.x, 16.x, 18.x]
        # See supported Node.js release schedule at https://nodejs.org/en/about/releases/

    steps:
    - uses: actions/checkout@v3
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}
        GlaDOSCookie: 'AWSALB=2g8LK48f+YRjYlfav7EMnM4yJBM/oDpHKCJqxyGkbmlz1H8YFctEH5IM1P1VqKIkx+ZzZVndYGGjNswyw2uawi4kdjlTqZBco58mrh6K2vSLONy0FUw5huZSnKYW; AWSALBCORS=2g8LK48f+YRjYlfav7EMnM4yJBM/oDpHKCJqxyGkbmlz1H8YFctEH5IM1P1VqKIkx+ZzZVndYGGjNswyw2uawi4kdjlTqZBco58mrh6K2vSLONy0FUw5huZSnKYW'
        cache: 'npm'
    - run: node index.js
